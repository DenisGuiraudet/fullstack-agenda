{
  "name": "@agenda/monorepo",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "front": "nx dev @agenda/front",
    "storybook": "nx storybook @agenda/storybook",
    "format": "nx run-many -t format"
  },
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "dependencies": {
    "@agenda/config": "workspace:*"
  },
  "devDependencies": {
    "@nx/js": "17.2.0",
    "nx": "17.2.0"
  }
}
