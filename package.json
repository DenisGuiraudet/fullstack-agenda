{
  "name": "@agenda/monorepo",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "back": "nx dev @agenda/back",
    "front": "nx dev @agenda/front",
    "front:debug": "nx dev:debug @agenda/front",
    "storybook": "nx storybook @agenda/storybook",
    "format": "nx run-many -t format"
  },
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "dependencies": {
    "@trpc/client": "^10.44.1",
    "@trpc/server": "^10.44.1"
  },
  "devDependencies": {
    "@nx/js": "17.2.0",
    "nx": "17.2.0"
  }
}
